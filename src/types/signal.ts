// types/signal.ts — Re-exports of signal catalog types.
//
// Types are derived from the Zod schemas in `../schemas/signal` per issue #673.

export type {
  ColorRampStop,
  RampInterpolation,
  ColorRamp,
  SignalDef,
  SignalConfig,
} from '../schemas/signal.js'
